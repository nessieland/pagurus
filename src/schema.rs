table! {
    users (id) {
        id -> Int4,
        username -> Varchar,
        display_name -> Varchar,
        password -> Varchar,
        email -> Varchar,
        created_at -> Varchar,
    }
}
